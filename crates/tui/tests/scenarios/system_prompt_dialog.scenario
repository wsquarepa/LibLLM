[setup]
size 100x30
db temp
api none

[steps]
type "/system"
key Enter
expect focus == SystemPromptDialog
expect screen contains "assistant"
expect screen contains "roleplay"
paste "Cargo.toml"
expect status_message == "System prompt import supports .txt files only."
